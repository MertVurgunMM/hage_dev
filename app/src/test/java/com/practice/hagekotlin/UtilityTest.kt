package com.practice.hagekotlin

/**
 * 1. isNetworkAvailable
 * 2. generateEmptyFile
 * 3. generate defined files ---> (categories, contents, logs)
 * 4. writeToFile ---> writes new data package into given filename
 * 5. updateContent ---> makes a new request for files (categories, contents, logs)
 *                       with stored user data & writes response into files.
 *                       !? It could be better to name it as refresh content data !?
 * 6. readJsonFromAsset ---> returns string file of a UTC-8 content
 */
class UtilityTest {
}